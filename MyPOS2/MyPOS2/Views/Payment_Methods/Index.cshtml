@model IEnumerable<MyPOS2.Data.Entity.PAYMENT_METHOD>

@{
    ViewBag.Title = "Index";
}

<h2>Methodes de paiement</h2>

<p>
    @Html.ActionLink("Créer nouveau", "Create") | @Html.ActionLink("Retour au menu", "Management", "Home")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.method)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.method)
        </td>
        <td>
            @Html.ActionLink("Editer", "Edit", new { id=item.idPaymentMethod }) |
            @Html.ActionLink("Détails", "Details", new { id=item.idPaymentMethod }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.idPaymentMethod })
        </td>
    </tr>
}

</table>
