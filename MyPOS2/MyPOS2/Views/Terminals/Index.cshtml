@model IEnumerable<MyPOS2.Data.Entity.TERMINAL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create") | @Html.ActionLink("Back to Menu", "Management", "Home")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameTerminal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SHOP.phone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameTerminal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SHOP.phone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTerminal }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTerminal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTerminal })
        </td>
    </tr>
}

</table>
<div>
    @Html.ActionLink("Back to Menu", "Management", "Home")
</div>
